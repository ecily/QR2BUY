#pragma once

// Deprecated compatibility note:
// Active TFT_eSPI setup headers now live in include/tft_setup_*.h and are
// injected per PlatformIO environment via "-include" in platformio.ini.
// Keep this file only so older references do not silently point to stale pins.
